// Default deny: add reviewed Cedar permit rules for Tier 2 tools.
// Example (replace the tool ID before enabling):
// permit(principal, action == Action::"invoke", resource == Tool::"approved_tool");
